{
  "name": "sukoon-cf",
  "version": "0.1.0",
  "private": true,
  "description": "Sukoon — Cloudflare Workers + Hono edge backend for the free mental wellness platform.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "seed": "node scripts/json-to-d1.mjs"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=20"
  }
}
